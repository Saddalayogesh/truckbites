server:
  port: 8084

spring:
  datasource:
    url: jdbc:h2:mem:menu_db;INIT=CREATE SCHEMA IF NOT EXISTS truckbites_menu_db
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

spring:
  cloud:
    openfeign:
      circuitbreaker:
        enabled: true

jwt:
  secret: ${JWT_SECRET}

eureka:
  client:
    enabled: false
