server:
  port: 8083

spring:
  cloud:
    openfeign:
      circuitbreaker:
        enabled: true

# Feign circuit breaker tuning: the default 1s TimeLimiter is too tight for
# cold-start JVM calls, causing transient 503s right after startup. Give
# calls more headroom while keeping circuit-breaking enabled.
resilience4j:
  timelimiter:
    configs:
      default:
        timeout-duration: 5s

razorpay:
  key-id: ${RAZORPAY_KEY_ID:rzp_test_TLzX3pgsX3VSku}
  key-secret: ${RAZORPAY_KEY_SECRET:y7oqLKCalRJf5oVTG7hweSKN}

jwt:
  secret: ${JWT_SECRET}
